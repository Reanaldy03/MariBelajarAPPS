import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maribelajar.app',
  appName: 'MariBelajar',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
